{
  "name": "zodexy",
  "version": "0.0.0-dev",
  "description": "Type-safe (de)serialization for Zod",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    "import": "./dist/esm/index.js",
    "default": "./dist/index.js"
  },
  "bugs": "https://github.com/brettz9/zodexy/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/brettz9/zodexy"
  },
  "keywords": [
    "zod",
    "serialization",
    "deserialization",
    "reflection"
  ],
  "author": "Gregor Weber<mail@watwa.re>",
  "license": "MIT",
  "scripts": {
    "test": "vitest --coverage --silent=false",
    "tsc": "tsc",
    "build": "rm -rf dist && tsc -p tsconfig-prod.json && cp schema.zodexy.json dist/schema.zodexy.json && tsup index.ts --format esm --legacy-output",
    "prepublish": "pnpm run build"
  },
  "peerDependencies": {
    "zod": "^4.0.x"
  },
  "dependencies": {
    "type-fest": "^4.41.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "prettier": "3.8.3",
    "tsup": "^8.5.1",
    "typescript": "6.0.3",
    "vitest": "4.1.11"
  },
  "files": [
    "dist/*"
  ]
}
